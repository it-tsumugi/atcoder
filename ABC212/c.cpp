#define _GLIBCXX_DEBUG
#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(long long i=0;i<n;++i)
typedef long long ll;

int main(){
    ll n,m;
    cin >> n >> m;
    vector<ll>a(n),b(m);
    
    rep(i,n)cin >> a.at(i);
    rep(i,m)cin >> b.at(i);


    return 0;
}
