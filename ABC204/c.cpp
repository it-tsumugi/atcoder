#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
typedef long long ll;

int main(void){
    int N,M;
    cin >> N >> M;
    int A[N],B[M];
    rep(i,M){
        cin >> A[i];
        cin >> B[i];
    }

    
    return 0;
}